{
  "name": "scoring-the-game--blockio",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "dependencies": {
    "@babel/runtime-corejs2": "^7.9.2",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/plugin-transform-runtime": "^7.9.0",
    "prettier": "^2.0.4",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "serve": "parcel -p 4321 index.html",
    "tsc-check": "tsc --noEmit --watch --project . ",
    "prettier-check": "prettier --single-quote --trailing-comma es5 --print-width 100 --list-different ./src/*.ts ./src/*.tsx",
    "prettier": "yarn prettier-check --write"
  }
}
